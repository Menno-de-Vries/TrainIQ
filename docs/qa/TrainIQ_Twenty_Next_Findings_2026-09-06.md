# Twenty further findings after PR #20

Base: `8526478b34674e4035d6e014fc919bfdeb5b7022`. Branch: `codex/twenty-next-audit-fixes`.
PR #20 was merged separately before this audit. This batch is PR-only; no release or unrelated cleanup.

Before-edit selection. All findings concern existing behavior; priority P2 except data-loss findings 1–9 and 11 (P1). Paths below are under `TrainIQ-Project/app/src/main/java/com/trainiq`.

| ID | Observed problem and affected flow | Expected behavior / smallest verification |
| --- | --- | --- |
| NEXT20-01 | `TrainIqRepository.addMeasurement` allocates IDs from delayed observed state despite Room auto-generation. Consecutive measurements collide. | Let Room allocate IDs; two writes without Flow wait and reopen retain both. |
| NEXT20-02 | `addWorkoutDay` allocates ID and order outside the transaction; rapid additions overwrite a day, and count-based ordering collides after deletion. | Allocate from current Room plan inside transaction; consecutive additions and order gaps. |
| NEXT20-03 | Exercise insertion builds exercise/plan/set IDs from observed state. Consecutive picker additions can replace each other's graph. | Serialize graph construction from current plan tables; duplicate-library reuse, independent sets and rollback. |
| NEXT20-04 | Single-set field updates copy stale set and parent targets. A quick weight edit can undo a preceding reps edit. | Transform the current stored set; successive independent fields remain after reopen. |
| NEXT20-05 | Add/delete/reorder set operations replace a collection built from stale observation. They can lose a new set or resurrect a deleted sibling. | Build collection mutations in the write transaction; interleaving add/delete/reorder tests. |
| NEXT20-06 | `withRoutineSetCountSynced` returns early for unchanged count and ignores the plan editor's reps/rest/load/RPE/type inputs. | Apply edited plan defaults to sets while preserving identity; same/increased/decreased counts. |
| NEXT20-07 | Removing an exercise rewrites active state from an old observation and removes legacy exercise keys instead of current plan keys. | Read active state inside transaction, retain other logs/timer and remove only matching draft/collapse keys; duplicate plans and reopen. |
| NEXT20-08 | Replacing an exercise upserts a stale whole plan, potentially reverting recent targets/order. | Change exercise identity on the current plan; preserve latest targets and active historical performed identity. |
| NEXT20-09 | Meal snapshots resolve foods/recipes from delayed observations, so immediate save after a library edit uses old nutrients or reports a missing item. | Resolve current ingredients and save snapshot in one transaction; immediate food/recipe edit-to-meal and missing-item rollback. |
| NEXT20-10 | Selecting a stale/deleted routine ID clears every active flag. | Reject nonexistent selection without changing the active routine; valid and missing ID. |
| NEXT20-11 | Deleting a day/routine cascades into the active session, silently discarding its logs outside the explicit discard flow. | Require finishing/discarding that active session first; reject affected deletion, allow unrelated deletion. |
| NEXT20-12 | Saving an explicitly edited but deleted food can create a replacement or overwrite another barcode match. | Reject a stale explicit edit; new scans still reuse barcodes, existing edits retain identity. |
| NEXT20-13 | Exercise history uses an uncaught `stateIn` flow and an endless placeholder on failure. | Explicit loading/error/retry/success with local regression and production UI recovery proof. |
| NEXT20-14 | Manual routine name uniqueness is checked only against delayed UI state. Rapid create/rename can persist duplicate names. | Enforce existing case-insensitive trimmed-name rule transactionally; competing create and rename rollback. |
| NEXT20-15 | Exercise-plan editor and parent selection use `remember`; recreation loses an unfinished edit. | Save selected plan ID and input fields; production state-restoration test including invalid input. |
| NEXT20-16 | Planned-set editor and parent selection use `remember`; recreation loses edited type and metrics. | Save selected set ID and draft; state-restoration test for type/metrics. |
| NEXT20-17 | Nutrition product/recipe/deletion writes catch cancellation as an ordinary failure and invoke error callbacks. | Propagate cancellation while clearing pending state; success/failure/cancellation/retry component proof. |
| NEXT20-18 | Settings reads selected import documents concurrently before the existing preview controller, allowing an older slow read to replace the newest preview. | Own latest document-read request and cancel/ignore older completion; slow-old/fast-new, failure and cancellation tests. |
| NEXT20-19 | Home chooses the first day even when empty and a later day has exercises; its CTA starts an empty workout. | Share first-startable-day selection for dashboard/next-workout lookup; empty-first, all-empty and ordering cases. |
| NEXT20-20 | Any message containing `mislukt` closes the AI routine form, losing the user's retry context even for unrelated errors. | Keep failed generation form open with inputs, close only on successful proposal; UI failure/retry and unrelated-message proof. |

Audit inspected onboarding/tour/navigation, Home, workout overview/library/plan/logger/history/completion, nutrition/library/recipes/meals/scanners, Progress, Coach/profile, Settings, domain use cases, Room graph/DAO/entities, AI fallback boundaries and existing tests. Prior findings excluded; stale saved Coach advice rejected as a candidate because the current UI intentionally labels its fingerprint as outdated.

Clean-base local `:app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed before changes (2m34s). Runtime evidence uses the agent-owned read-only API 36 AVD, serial `emulator-5586`, alternate adb server 5038. No physical-performance or live-AI claim.

Implementation: all 20 selected changes are present. Combined local verification passed: `:app:assembleDebug :app:testDebugUnitTest :app:lintDebug :app:connectedDebugAndroidTest` (3m38s), 889 JVM tests and 87 selected Android tests. Lint reports 0 errors and 65 warnings, matching the clean-main baseline.

The connected class filter covers `WorkoutCompletionRecoveryInstrumentedTest`, `ActiveWorkoutSetActionsInstrumentedTest`, `ActiveWorkoutRestoreInstrumentedTest`, `GeneratedRoutinePreviewInstrumentedTest`, `WorkoutAiRoutineGenerationStateRestorationInstrumentedTest`, `CoachProfileStateRestorationInstrumentedTest`, `NutritionAiResultStateRestorationInstrumentedTest`, `NutritionLongFormImeInstrumentedTest`, `ScannerRecoveryInstrumentedTest`, `FeatureRecoveryInstrumentedTest`, `TargetedRoomPersistenceInstrumentedTest`, `TrainIqDatabaseMigrationTest`, `CoordinatorMutationInstrumentedTest`, `PlanDraftRestorationInstrumentedTest`, and `ExercisePlanValidationInstrumentedTest`. Tests use the configured Gradle connected route, not direct instrumentation. Local reports remain ignored under `TrainIQ-Project/app/build/reports/`; UI trees/captures remain ignored under `artifacts/next-twenty/`.

Manual emulator walkthrough covered onboarding/skip/tour, navigation, empty training, nutrition, Coach, Settings, Progress, creating a synthetic routine, adding its first exercise, opening the plan editor and planned-set editor, and scrolling to the set save action. Visual inspection used 720x1280 with font scale 1.3; the plan dialog actions and scrolled set-save action were reachable. The crash buffer was empty during this walkthrough. Production Compose tests separately cover restoration, invalid inputs, retry/back and affected recovery flows.

Limits: this is selected affected Android coverage, not a claim that the entire instrumentation suite passes. Earlier clean-main evidence recorded unrelated failures in CoachInsights, ExerciseHistory, two AiCameraScannerModes cases and CameraPermissionScanner; they are not included in this passing selection. No physical performance, live AI, Health Connect permission matrix, SAF provider end-to-end, TalkBack, tablet/foldable or full accessibility certification is claimed. No schema, dependencies, release configuration or CI workflow changed. Final commit identity and post-commit gate results are recorded in the PR.

Review decisions: planning transactions read only the plan tables and write the affected graph/rows; they never mirror JSON or touch unrelated history/nutrition. Changing only a set count preserves individually configured existing sets; a changed plan-default field applies that field without resetting other per-set values. Removing a plan preserves another plan's draft even when its ID matches a legacy exercise key. Active-plan removal returns a dedicated safe, actionable error. Editor restoration stores IDs and scalar/enum drafts rather than serializing live database graphs.

Regression evidence includes failing-before-fix Room tests for routine selection/names/stale food edits, current set mutations, day ordering, exercise additions and active-plan deletion/removal; actual-coordinator tests reproduced measurement primary-key collisions and stale meal nutrients. Production Compose tests reproduced discarded plan/set drafts and failure-driven AI form dismissal. Controlled coroutine tests reproduced nutrition cancellation callbacks, out-of-order import reads and unrecoverable exercise-history observation. Two existing architecture source guards were adapted to the new internal method boundary/trailing-lambda call while retaining their single-state/targeted-write invariants and adding a prohibition on observed-state mutation inputs.

The debug-only existing test entry point additionally exposes the real coordinator for end-to-end persistence regression tests; release code and dependency injection remain unchanged.
